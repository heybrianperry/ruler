<rule_activation id="636e8efa-a20b-47a9-953a-ece4ba7555b7" title="Adopt TypeScript with Jest/Vitest for Unit Testing Framework: Test Files Written" applies_to="tests/unit/**/*.test.ts">
These rules are ALWAYS ACTIVE for all TypeScript test file development and test framework configuration.
</rule_activation>

### Rules

- **R-TS-TEST-001** MUST: Test files MUST be written in TypeScript to leverage type safety and maintain consistency with the application codebase.

### Verify

```bash
# Count TypeScript test files in tests/unit directory
find tests/unit -name '*.test.ts' -type f | wc -l

# Verify test file structure and content
grep -r "describe\|test\|it" tests/unit/**/*.test.ts | head -n 5

# Check TypeScript compilation for test files
npx tsc --noEmit --project tsconfig.test.json 2>&1 | grep -c 'error' || echo '0'
```

**Accept when:**
- All test files in tests/unit/ directory use the .test.ts extension and are discoverable by the test runner
- TypeScript compilation succeeds for all test files without type errors
- Test runner configuration correctly identifies and executes all .test.ts files, with no orphaned or undiscovered tests

<enforcement>
Claude Code MUST NOT skip or defer verification of TypeScript test file conventions. Violations block CI builds and pull requests until remediated.
</enforcement>