<rule_activation id="bb07ff99-4472-4089-95ce-a1dbfc62dc71" title="Standardize Cache Layer Configuration Management Pattern: Cache Layer Configurations" applies_to="**/*">
These rules are ALWAYS ACTIVE for all runtime environment configuration and cache layer implementations. All configuration management code MUST comply with these rules.
</rule_activation>

### Rules

- **R-CACHE-001** MUST: All cache layer configurations MUST be managed through a centralized configuration pattern that is accessible across runtime contexts.

### Verify

```bash
# Verify cache configuration pattern compliance
grep -r 'cache.*config' src/ --include='*.ts' | grep -E '(propagate|settings)' || echo 'Pattern not found'

# Find cache configuration files
find src/ -name '*settings*.ts' -o -name '*propagate*.ts' | xargs grep -l 'cache' || echo 'No cache configuration files found'

# Run cache configuration tests
npm test -- --grep 'cache.*configuration' || echo 'No cache configuration tests found'
```

**Accept when:**
- All cache layer configuration code follows the standardized pattern detected in IR signature 3233fb2b7f24bb1c444af07faab034c3
- Configuration propagation between MCP contexts and settings is consistent and validated
- Unit tests verify cache configuration behavior across all runtime contexts
- Cache configuration in MCP propagation modules (propagateOpenHandsMcp.ts) follows the data.cache_layer facet pattern
- Cache-related settings in VSCode extension configuration follow the centralized pattern
- Runtime environment configuration affecting cache behavior is managed through the centralized pattern
- Configuration propagation between application boundaries is validated and consistent

<enforcement>
Claude Code MUST NOT skip or defer verification. Automated code review checks for cache configuration patterns in pull requests are mandatory. CI pipeline verification commands that detect deviations from the standard pattern must pass. Architecture review is required for any new cache layer implementations. Pull requests with non-compliant cache configuration patterns are blocked until corrected.
</enforcement>