<rule_activation id="d8de7f22-10cd-411b-85a8-df13f8da6cbd" title="Standardize Unit Testing Structure for Agent Components: Agent Unit Test" applies_to="**/*">
These rules are ALWAYS ACTIVE for all agent component development and testing activities.
</rule_activation>

### Rules

- **R-AGENT-001** MUST: Agent unit test files MUST follow the naming convention {AgentName}.test.ts where {AgentName} matches the agent class name

### Verify

```bash
# Verify agent test directory exists
test -d tests/unit/agents && echo 'Agent test directory exists' || echo 'Missing agent test directory'

# Count agent test files
find tests/unit/agents -name '*.test.ts' | wc -l

# List all agent test files
ls tests/unit/agents/*.test.ts 2>/dev/null || echo 'No agent tests found'
```

**Accept when:**
- The tests/unit/agents/ directory exists and contains .test.ts files
- Each agent implementation class has a corresponding {AgentName}.test.ts file in tests/unit/agents/
- Test files can be discovered and executed by the project's test runner without additional configuration
- All agent classes (OpenCodeAgent, WarpAgent, WindsurfAgent, JunieAgent, JulesAgent, etc.) have corresponding unit test files following the naming convention

<enforcement>
Claude Code MUST NOT skip or defer verification of agent unit test file naming conventions and directory structure. Violations must be reported and remediated before proceeding.
</enforcement>