<rule_activation id="b625bebb-0929-442a-a2aa-c60865d5a15d" title="Adopt TypeScript with Jest/Vitest for Unit Testing Framework: Unit Test Files" applies_to="**/*.test.ts">
These rules are ALWAYS ACTIVE for all TypeScript test file development and test framework configuration.
</rule_activation>

### Rules

- **R-TS-TEST-001** MUST: All unit test files MUST use the .test.ts extension to enable automatic test discovery and execution by the test runner.

### Verify

```bash
# Count all .test.ts files in tests/unit directory
find tests/unit -name '*.test.ts' -type f | wc -l

# Verify test structure with describe/test/it patterns
grep -r "describe\|test\|it" tests/unit/**/*.test.ts | head -n 5

# Check TypeScript compilation succeeds for test files
npx tsc --noEmit --project tsconfig.test.json 2>&1 | grep -c 'error' || echo '0'
```

**Accept when:**
- All test files in tests/unit/ directory use the .test.ts extension and are discoverable by the test runner
- TypeScript compilation succeeds for all test files without type errors
- Test runner configuration correctly identifies and executes all .test.ts files, with no orphaned or undiscovered tests

<enforcement>
Claude Code MUST NOT skip or defer verification of test file naming conventions and TypeScript compilation. Violations block CI builds and pull requests until resolved.
</enforcement>