<rule_activation id="c3b75e04-71c9-4a21-9a3d-e4753b4d30d8" title="Standardize Unit Testing Framework for Agent Components: Agent Implementations Have" applies_to="**/*">
These rules are ALWAYS ACTIVE for all agent component development and testing activities within the codebase.
</rule_activation>

### Rules

- **R-TEST-001** MUST: All agent implementations MUST have corresponding unit test files following the naming convention `tests/unit/agents/{AgentName}.test.ts`

### Verify

```bash
# Verify test files follow naming pattern and are in correct location
find tests/unit -name '*.test.ts' | grep -E 'tests/unit/(agents|cli)/.+\.test\.ts$'

# Verify code coverage meets minimum threshold
npm test -- --coverage --coverageThreshold='{"global":{"lines":80}}'

# Verify at least 6 agent test files exist
ls tests/unit/agents/*.test.ts | wc -l | grep -E '^[6-9]|^[1-9][0-9]+$'
```

**Accept when:**
- All test files follow the naming pattern `*.test.ts` and are located in appropriate subdirectories under `tests/unit/`
- Test suite executes successfully with at least 80% code coverage across agent components
- At least 6 agent test files exist in `tests/unit/agents/` directory, maintaining the established pattern

<enforcement>
Claude Code MUST NOT skip or defer verification. All agent implementations require corresponding unit tests in the standardized location before code can be approved.
</enforcement>