<rule_activation id="21ce38af-4f7c-47f9-9430-310fe79e106d" title="Adopt TypeScript with Jest/Vitest for Unit Testing Framework: Each Module Class" applies_to="**/*.test.ts">
These rules are ALWAYS ACTIVE for all TypeScript test file development and test framework configuration.
</rule_activation>

### Rules

- **R-TS-TEST-001** MUST: Each module or class MUST have a corresponding test file with the same base name followed by .test.ts (e.g., AmpAgent.ts → AmpAgent.test.ts).

### Verify

```bash
# Count all test files in the tests/unit directory
find tests/unit -name '*.test.ts' -type f | wc -l

# Verify test structure and content
grep -r "describe\|test\|it" tests/unit/**/*.test.ts | head -n 5

# Check TypeScript compilation succeeds for all test files
npx tsc --noEmit --project tsconfig.test.json 2>&1 | grep -c 'error' || echo '0'
```

**Accept when:**
- All test files in tests/unit/ directory use the .test.ts extension and are discoverable by the test runner
- TypeScript compilation succeeds for all test files without type errors
- Test runner configuration correctly identifies and executes all .test.ts files, with no orphaned or undiscovered tests

<enforcement>
Claude Code MUST NOT skip or defer verification. All TypeScript test files MUST follow the .test.ts naming convention and be properly organized in the tests/unit/ directory hierarchy. Violations block CI builds and pull requests until remediated.
</enforcement>