<rule_activation id="8f18dd5d-533d-4d7d-bf5f-029ed884d99d" title="Standardize Unit Testing Framework for Agent Components: Unit Tests Placed" applies_to="**/*">
These rules are ALWAYS ACTIVE for all agent component development and testing activities within the codebase.
</rule_activation>

### Rules

- **R-UNIT-001** MUST: Unit tests MUST be placed in the `tests/unit/` directory structure, organized by component type (agents, cli, etc.)

### Verify

```bash
# Verify test files follow naming pattern and are in correct directory structure
find tests/unit -name '*.test.ts' | grep -E 'tests/unit/(agents|cli)/.+\.test\.ts$'

# Verify code coverage meets minimum threshold
npm test -- --coverage --coverageThreshold='{"global":{"lines":80}}'

# Verify at least 6 agent test files exist to maintain established pattern
ls tests/unit/agents/*.test.ts | wc -l | grep -E '^[6-9]|^[1-9][0-9]+$'
```

**Accept when:**
- All test files follow the naming pattern `*.test.ts` and are located in appropriate subdirectories under `tests/unit/`
- Test suite executes successfully with at least 80% code coverage across agent components
- At least 6 agent test files exist in `tests/unit/agents/` directory, maintaining the established pattern

<enforcement>
Claude Code MUST NOT skip or defer verification. All pull requests adding new agents or CLI handlers must include corresponding unit tests in `tests/unit/` with the correct directory structure and naming conventions. Violations will fail CI checks and block merging.
</enforcement>